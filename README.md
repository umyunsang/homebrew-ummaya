# Homebrew UMMAYA

Homebrew tap for UMMAYA.

```bash
brew tap umyunsang/ummaya
brew install ummaya
```

CLI cask install:

```bash
brew install --cask ummaya
```

One-line install:

```bash
brew install umyunsang/ummaya/ummaya
```
